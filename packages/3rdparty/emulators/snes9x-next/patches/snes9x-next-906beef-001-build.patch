diff -rupN snes9x-next.git.orig/Makefile.libretro snes9x-next.git/Makefile.libretro
--- snes9x-next.git.orig/Makefile.libretro	2012-12-30 17:17:28.778451232 -0500
+++ snes9x-next.git/Makefile.libretro	2012-12-30 17:16:44.290725029 -0500
@@ -25,8 +25,6 @@ else ifneq ($(findstring MINGW,$(shell u
    system_platform = win
 endif
 
-CC         = gcc
-
 ifeq ($(platform), unix)
    TARGET := libretro.so
    fpic := -fPIC
